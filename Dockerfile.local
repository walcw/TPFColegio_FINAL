FROM eclipse-temurin:21-jdk
WORKDIR /app
# Copiar cualquier JAR generado en target/ (más tolerante a cambios de versión)
COPY target/*.jar /app/app.jar
EXPOSE 8080
ENTRYPOINT ["java","-jar","/app/app.jar"]
